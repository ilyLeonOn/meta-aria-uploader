# meta-aria-uploader
Process Meta Aria VRS recordings via Meta's MPS pipeline (SLAM, gaze, hand‑tracking) and upload processed outputs to Google Cloud Storage. GUI with configurable concurrency, save/load credentials, progress/debug console, and modes: Convert+Upload, Convert Only, Upload Only. Requires Python 3.12.
